# 生产环境配置示例

# 环境设置
NODE_ENV=production
PORT=3000

# 数据库配置
DB_HOST=your-database-host
DB_USER=your-database-user
DB_PASSWORD=your-database-password
DB_NAME=campus_wall
DB_PORT=3306

# Redis配置
REDIS_HOST=your-redis-host
REDIS_PORT=6379
REDIS_PASSWORD=your-redis-password
REDIS_DB=0

# JWT配置
JWT_SECRET=your-super-secure-jwt-secret-key-here
JWT_EXPIRE=7d

# 文件上传配置
UPLOAD_DIR=/var/www/uploads
MAX_FILE_SIZE=5242880
UPLOAD_URL_STRATEGY=relative

# 服务器域名配置（生产环境必填）
SERVER_DOMAIN=your-domain.com
USE_HTTPS=true
STATIC_PATH=/uploads

# CORS配置
CORS_ORIGIN=https://your-frontend-domain.com

# 示例配置说明：
# 1. SERVER_DOMAIN: 你的生产环境域名，如 api.example.com
# 2. USE_HTTPS: 生产环境建议使用 HTTPS
# 3. UPLOAD_URL_STRATEGY: 
#    - relative: 存储相对路径，推荐用于生产环境
#    - absolute: 存储绝对路径，仅用于特殊情况
# 4. CORS_ORIGIN: 前端域名，确保安全性
