// 应用配置
const config = {
  // API配置
  API_BASE_URL: 'http://localhost:3000/api',

  // 服务器基础URL
  SERVER_BASE_URL: 'http://localhost:3000',
};

// 导出配置
export default config;

// 导出具体配置项
export const { API_BASE_URL, SERVER_BASE_URL } = config;
